// varscope function (named_fn) accesses_outer:
// console, x,
// varscope function (unnamed) accesses_outer:
// console, x,

// best guess?:
// for every hoist-scope (global, function):
//   scope.accesses/refs is array with
//   all references inside that scope
//   references inside sub-scopes can easily be collected
//   by recursing on scope.children
// also refsNames? (array of names instead of nodes so no dups)

